const { Router } = require("express");
const courseRoute = Router();

courseRoute.post("/purchase", function (req, res) {});
courseRoute.post("/review", function (req, res) {});

module.exports = courseRoute;
